@page
@model WebApplication1.Pages.Account.ForgotPasswordModel

<h2>If an account with that email exists, a reset link has been sent.</h2>

@if (TempData["ResetLink"] != null)
{
    <div class="alert alert-info">
        Development-only reset link: <a href="@TempData["ResetLink"]">@TempData["ResetLink"]</a>
    </div>
}
